{
  "name": "iptime-wol",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "wrangler dev --ip 0.0.0.0 --port 3000",
    "start": "npm test",
    "secret": "wrangler secret:bulk secrets.json"
  },
  "devDependencies": {
    "wrangler": "^3.56.0"
  },
  "dependencies": {
    "@vespaiach/axios-fetch-adapter": "^0.3.1",
    "axios": "^0.27.2"
  }
}
